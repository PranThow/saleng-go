// config.example.js — copy this file to config.js and fill in your keys.
// config.js is gitignored so your tokens never reach GitHub.
//
// For GitHub Pages deployment, add MAPBOX_TOKEN as a repository secret
// and the GitHub Actions workflow will auto-generate config.js on deploy.

window.APP_CONFIG = {
  mapboxToken: 'YOUR_MAPBOX_PUBLIC_TOKEN_HERE',
};
